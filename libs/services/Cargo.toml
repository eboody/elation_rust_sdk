[package]
name = "services"
version = "0.1.0"
edition = "2021"

[dependencies]
client = { path = "../client" }
models = { path = "../models" }
error = { path = "../error" }

serde = { workspace = true }
